name: Test

on:
  push:
    branches: [ master ]

  pull_request:
    branches: [ master ]

  workflow_dispatch:
  workflow_call:
    outputs:
      artifact_name:
        value: ${{ jobs.fresh_build.outputs.artifact_name }}
      artifact_run_id:
        value: ${{ jobs.fresh_build.outputs.run_id }}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:

  setup:
    runs-on: ubuntu-latest

    steps:
      - name: Fetch Supported Python Versions
        id: fetch-versions
        run: |
          versions=$(curl -fsSL https://endoflife.date/api/v1/products/python/ \
                     | jq '[ .result.releases[] | select(.isEol == false) | .label ]')

          echo "supported_versions=$(echo $versions | jq -c '.')"        >> $GITHUB_OUTPUT
          echo "bound_versions=$(echo $versions | jq -c '[.[0],.[-1]]')" >> $GITHUB_OUTPUT

    outputs:
      supported_versions: ${{ steps.fetch-versions.outputs.supported_versions }}
      bound_versions:     ${{ steps.fetch-versions.outputs.bound_versions }}

  fresh_build:
    uses: ./.github/workflows/dist_build.yml

  testing:
    runs-on: ${{ matrix.os }}

    permissions:
      id-token: write

    strategy:
      matrix:
        os: [ ubuntu-latest ]
        py-version: ${{ fromJson(needs.setup.outputs.supported_versions) }}
        include:
          - os: windows-latest
            py-version: ${{ fromJson(needs.setup.outputs.bound_versions)[0] }}
          - os: windows-latest
            py-version: ${{ fromJson(needs.setup.outputs.bound_versions)[1] }}
          - os: macos-latest
            py-version: ${{ fromJson(needs.setup.outputs.bound_versions)[0] }}
          - os: macos-latest
            py-version: ${{ fromJson(needs.setup.outputs.bound_versions)[1] }}

    needs:
      - fresh_build
      - setup

    steps:
      - uses: actions/checkout@v7
        with:
          fetch-depth: 0

      # The moment equations are FORTRAN 77 compiled by f2py, so every test
      # runner needs a Fortran compiler. Linux images ship gfortran; macOS
      # and Windows do not reliably.
      - name: Install gfortran (macOS)
        if: runner.os == 'macOS'
        run: |
          brew install gcc || brew link --overwrite gcc
          echo "$(brew --prefix gcc)/bin" >> "$GITHUB_PATH"

      - name: Install gfortran (Windows)
        if: runner.os == 'Windows'
        shell: bash
        run: |
          command -v gfortran >/dev/null 2>&1 || choco install -y mingw

      - name: Show Fortran compiler
        shell: bash
        run: gfortran --version

      - uses: astral-sh/setup-uv@v7
        with:
          python-version: ${{ matrix.py-version }}

      # Built from source rather than from the shared artifact: the wheel is
      # now platform- and interpreter-specific, so a single artifact cannot
      # be installed across this matrix.
      - name: Install Packages
        shell: bash
        run: |
          uv sync --no-dev --group test
          uv pip install -v .

      - name: Run Tests
        run: uv run --no-sync pytest -v --cov --cov-report=xml tests

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v7
        with:
          fail_ci_if_error: true
          use_oidc: true
          verbose: true
